import React from 'react';
import logo from '/src/images/logo.png'; // Update path if needed

const Logo = () => (
  <img
    src={logo}
    alt="Logo"
    width="150" // Adjust size as needed
    height="auto"
  />
);

export default Logo;
